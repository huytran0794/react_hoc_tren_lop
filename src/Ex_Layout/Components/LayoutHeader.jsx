import React from "react";
import styles from "../ex_layout.module.css";
export default function LayoutHeader() {
  return (
    <div className={`${styles["title-color"]} bg-primary`}>LayoutHeader</div>
  );
}
